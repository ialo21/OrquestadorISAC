import BotPage from './BotPage'

export default function RobotExtraccionMongoPage() {
  return (
    <BotPage botId="robot-extraccion-mongo">
      {/* Parametrización específica del bot — pendiente de implementar */}
      <p className="text-sm text-gray-400 italic">
        Parámetros configurables del Robot Extracción MongoDB (a implementar).
      </p>
    </BotPage>
  )
}
