import BotPage from './BotPage'

export default function RPAMoniObjetosPage() {
  return (
    <BotPage botId="rpa-moni-objetos">
      {/* Parametrización específica del bot — pendiente de implementar */}
      <p className="text-sm text-gray-400 italic">
        Parámetros configurables del RPA Monitoreo Objetos (a implementar).
      </p>
    </BotPage>
  )
}
